info:
  component: Config Server
spring.cloud:
    config:
      server:
        git:
          uri: https://github.com/duvalhub/config-repo

spring.application:
    name: configserver

management.endpoints.web.exposure.include: "*"

spring.jackson.serialization.indent_output: true
